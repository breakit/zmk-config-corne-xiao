# Peripheral (right) half specific Kconfig overrides.

# WPM is a central-half display feature; the peripheral role does not wire up
# the keycode events it depends on, which caused a link error. Keep it off here
# (the shared config/corne_xiao_v1.conf enables it for the central display).
CONFIG_ZMK_WPM=n
