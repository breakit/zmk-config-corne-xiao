# Use NFC pads as GPIOS
CONFIG_NFCT_PINS_AS_GPIOS=y

# Enable deep sleep
CONFIG_ZMK_SLEEP=y

# Full consumer application-control usages, used by the "Buttons" layer
# shortcuts (Launchpad, Mission Control, Spotlight).
CONFIG_ZMK_HID_CONSUMER_APPLICATION_CONTROL=y
